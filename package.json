{
  "name": "@hub-spoke/workspace",
  "private": true,
  "description": "Monorepo for Hub & Spoke Content Manager",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev:cli": "npm run dev -w @hub-spoke/cli",
    "dev:api": "npm run dev -w @hub-spoke/api"
  },
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}
